provider "aws" {
  region = "us-west-2"  # Specify your desired AWS region
}

resource "aws_instance" "example_ec2" {
  ami           = "ami-0c55b159cbfafe1f0"  # Specify the ID of the AMI you want to use
  instance_type = "t2.micro"                # Specify the instance type

  # Specify other instance attributes as needed, such as tags, security groups, etc.
  
  # Example tags
  tags = {
    Name = "example-instance"
  }
  
  # Example security groups (replace sg-xxxxxxxx with your actual security group ID)
  security_groups = ["sg-xxxxxxxx"]
}
